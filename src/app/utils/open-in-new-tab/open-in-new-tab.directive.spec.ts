import { OpenInNewTabDirective } from './open-in-new-tab.directive';

describe('OpenInNewTabDirective', () => {
  it('should create an instance', () => {
    const directive = new OpenInNewTabDirective();
    expect(directive).toBeTruthy();
  });
});
